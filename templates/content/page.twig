<article id="post-{{ post.id }}" class="{{ post.class }}">
  {#
    /**
     * Functions hooked in to storefront_page add_action
     *
     * @hooked storefront_page_header          - 10
     * @hooked storefront_page_content         - 20
     */
  #}
  {% do action('storefront_page') %}
</article><!-- #post-{{ post.id }}# -->
