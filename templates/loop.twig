{% do action('storefront_loop_before') %}

{% for post in posts %}
  {% include ['content/' ~ post.format ~ '.twig', 'content/default.twig'] with { post: post } only %}
{% endfor %}

{% do action('storefront_loop_after') %}
